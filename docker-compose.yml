services:
  discord-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=America/Vancouver
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
